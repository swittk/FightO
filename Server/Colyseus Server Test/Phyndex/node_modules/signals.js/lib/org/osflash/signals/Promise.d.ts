import { ISlot } from "./ISlot";
import { OnceSignal } from "./OnceSignal";
export declare class Promise extends OnceSignal {
    private isDispatched;
    private valueObjects;
    /** @inheritDoc */
    addOnce(listener: Function): ISlot;
    /**
     * @inheritDoc
     * @throws flash.errors.IllegalOperationError <code>IllegalOperationError</code>: You cannot dispatch() a Promise more than once
     */
    dispatch(...valueObjects: any[]): void;
}
